services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ag-playable-2048:latest
    container_name: ag-playable-2048
    ports:
      - "${HOST_PORT:-8080}:${CONTAINER_PORT:-80}"
    restart: unless-stopped
